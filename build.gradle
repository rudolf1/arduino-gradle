apply from: 'arduino.gradle'

ext.libraries = [
    // "SoftwareSerial"
    "SPI"
]

// ext.defaultBoard = "adafruit:samd:adafruit_feather_m0"
ext.defaultBoard = "arduino:avr:uno"
ext.boards = [
    "arduino:avr:uno",,
    "adafruit:avr:feather32u4",
    "adafruit:samd:adafruit_feather_m0"
]
ext.projectName = "Blink.ino"
ext.projectLibrariesDir = "$projectDir/libraries"
ext.arduinoPackagesDir = "C:/Source/okavango/arduino"

// vim: set ft=groovy:

